{
  "name": "railway-static-site",
  "version": "1.0.0",
  "description": "Simple static HTML site for Railway",
  "scripts": {
    "start": "serve -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
